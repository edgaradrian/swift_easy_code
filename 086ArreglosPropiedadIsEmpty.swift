//Arreglo con uso de propiedad isEmpty

import Cocoa

var miArregloA = [Int] (repeating: 2, count: 2)
var miArregloB = [Int] (repeating: 3, count: 2)

var miArregloC = [Int] ()

//Validamos si esta vacio
print("¿miArregloA es vacio? \(miArregloA.isEmpty)")
print("¿miArregloB es vacio? \(miArregloB.isEmpty)")
print("¿miArregloC es vacio? \(miArregloC.isEmpty)")
